---
export interface Props {
  title: string;
  defaultOpen?: boolean;
}

const { title, defaultOpen = false } = Astro.props;
---

<div data-default-open={defaultOpen}>
  <h3>{title}</h3>
  <div>
    <slot />
  </div>
</div>
